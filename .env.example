# API Key for authentication (set in Vercel Project Settings -> Environment Variables)
API_KEY=your_secret_api_key_here_change_me_in_production

# MongoDB connection string (replace <password> and <dbname>)
MONGODB_URI=mongodb+srv://nour:<db_password>@cluster0.uho3gyu.mongodb.net/
MONGODB_DB=damietta

# For Vercel: set these in Project Settings -> Environment Variables
# API_KEY (required)
# MONGODB_URI (required)
# MONGODB_DB (optional)
